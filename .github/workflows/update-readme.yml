name: Auto Update README

on:
  schedule:
    - cron: '0 * * * *'  # every hour
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          persist-credentials: false  # critical for PAT

      - name: Update README timestamp
        run: |
          TODAY=$(date '+%Y-%m-%d-%H:%M:%S')  # replace spaces with dash
          sed -i "s/last-updated-[^ ]*/last-updated-$TODAY/" README.md

      - name: Commit & Push
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add README.md
          git commit -m "Auto-update README timestamp: $TODAY" || echo "No changes to commit"
          git push https://x-access-token:$PAT_TOKEN@github.com/${{ github.repository }} HEAD:main
